{
  "request": {
    "request_id": "live-turn-ten-step-001",
    "system": "这是一个真实的多步骤工具测试，完成条件是用户清单中的十项查询全部得到工具结果。工具的精确名称是 shell.query（包含英文句点），必须原样使用，不能写成 shell_query 或其他别名。维护已完成任务编号：第一次执行第1项；每收到一次工具结果，只将对应的一项标记完成，再选择最小的未完成编号。每次 assistant 回复必须且只能调用一个工具，调用前用简短文字标记当前编号，例如 STEP 1/10；禁止把多个查询合并成一批。收到 STEP 9/10 的结果后必须执行 STEP 10/10（Cargo.toml 行数），不能在第九项后总结。只有收到第十项的工具结果后，下一次 assistant 回复才可以总结结束。不要跳过任务，不要用文字代替实际调用。",
    "messages": [
      {
        "role": "user",
        "content": [
          {
            "type": "text",
            "text": "请严格按顺序完成以下十项任务，每项都必须调用一次 shell.query，完成第十项后再总结：1) 统计 tests 目录条目数；2) 统计 tests/core 目录条目数；3) 统计 tests/provider 目录条目数；4) 统计 tests/fixtures 目录条目数；5) 统计 tests/core/step.rs 行数；6) 统计 tests/core/turn.rs 行数；7) 统计 tests/provider/openai_compat.rs 行数；8) 统计 tests/provider/anthropic_compat.rs 行数；9) 统计 README.md 行数；10) 统计 Cargo.toml 行数。每次只调用一个工具。"
          }
        ]
      }
    ],
    "tools": [
      {
        "name": "shell.query",
        "description": "执行受限的文件查询，只支持 pwd、list、count_lines、count_entries。",
        "input_schema": {
          "type": "object",
          "properties": {
            "command": {
              "type": "string",
              "enum": ["pwd", "list", "count_lines", "count_entries"]
            },
            "path": { "type": "string" }
          },
          "required": ["command", "path"],
          "additionalProperties": false
        }
      }
    ],
    "tool_choice": "auto",
    "max_output_tokens": 80960
  },
  "expectations": {
    "stop_reason": "end_turn",
    "text": { "min_chars": 1 },
    "requires": []
  },
  "turn": {
    "min_steps": 11,
    "min_tool_calls": 10,
    "max_steps": 12
  }
}
